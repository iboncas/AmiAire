# Iteration 1

This folder groups the assets from the first clustering iteration.

Contents:

- `analysis/`: downstream clustering, selection, explainability, taxonomy, and context scripts
- `output/`: generated datasets, PCA outputs, clustering results, and interpretation artifacts
- `docs/`: planning and execution notes for the first iteration

The iteration-1 analysis scripts now default to paths inside `iteration1/output/`.
